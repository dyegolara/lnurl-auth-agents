# Dependencies are run-time vendored under lib/vendor/ (no `npm install`
# needed at runtime). node_modules is only for dev tooling (vitest, bech32
# reference) and must never be committed.
node_modules/

.DS_Store
*.log
*.key

# ClawHub Plugin Inspector output
reports/